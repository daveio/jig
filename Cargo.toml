[package]
name = "jig"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.40", features = [
  "derive",
  "cargo",
  "env",
  "unicode",
  "wrap_help",
  "string",
] }
ratatui = { version = "0.29.0", features = [
  "crossterm",
  "serde",
  "macros",
  "palette",
  "all-widgets",
  "underline-color",
  "document-features",
  "scrolling-regions",
] }
